<div class="row">
    <div class="col-md-6">
        <p>{{burger_name}}</p>
    </div>
    <div class="col-md-6">
        <button class="devoured" data-id="{{id}}" data-devoured={{devoured}}>
          Devoured  
        </button>
    </div>
</div>