<div class="row">
    <div class="col-md-6">
        <p>{{burger_name}}</p>
    </div>
    <div class="col-md-6">
        <button class="devoured btn btn-danger" data-id="{{id}}" data-devoured={{devoured}}>
          {{#if devoured}}Trash It{{else}}Devour It!{{/if}}
        </button>
    </div>
</div>